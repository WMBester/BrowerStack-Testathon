{
  "name": "testathon-playwright",
  "version": "1.0.0",
  "description": "Playwright tests for testathon.live",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "browserstack-node-sdk": "^1.49.10"
  }
}
